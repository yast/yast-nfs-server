#
# Makefile.cvs
#
configure: all
	./configure

all:
	y2tool y2automake
	aclocal
	autoconf
	automake -a -f

install: configure
	make
	make install

reconf: all
	./config.status --recheck
	./config.status
